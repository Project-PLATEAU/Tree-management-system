import React from "react"
import { IconButton } from "@mui/material"
import { Home as HomeIcon } from "@mui/icons-material"

const tableIconButtonRenderer = () => {
  return (
    <IconButton>
      <HomeIcon />
    </IconButton>
  )
}
